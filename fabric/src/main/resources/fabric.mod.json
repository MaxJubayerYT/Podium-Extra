{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_authors}"
  ],
  "contact": {
    "sources": "https://github.com/RaydanOMGr/Podium"
  },
  "license": "${mod_license}",
  "icon": "assets/podium/icon.png",
  "environment": "client",
  "mixins": [
    "podium.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "${minecraft_version_range}",
    "java": ">=25",
    "sodium": "${sodium_version_range}"
  }
}
